<%= form_with(model: usrfav, local: true) do |form| %>
  <% if usrfav.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(usrfav.errors.count, "error") %> prohibited this usrfav from being saved:</h2>

      <ul>
      <% usrfav.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id %>
  </div>

  <div class="field">
    <%= form.label :route_id %>
    <%= form.text_field :route_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
