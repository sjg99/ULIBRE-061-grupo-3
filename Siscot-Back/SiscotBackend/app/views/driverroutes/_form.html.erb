<%= form_with(model: driverroute, local: true) do |form| %>
  <% if driverroute.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(driverroute.errors.count, "error") %> prohibited this driverroute from being saved:</h2>

      <ul>
      <% driverroute.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :driver_id %>
    <%= form.text_field :driver_id %>
  </div>

  <div class="field">
    <%= form.label :route_id %>
    <%= form.text_field :route_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
